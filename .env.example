# OVM Pro - Environment Variables Configuration
# 
# ⚠️  REPLIT USERS: DO NOT create a .env file!
# Use Replit's built-in Secrets tab in the Tools panel to add environment variables.
# This is more secure and prevents accidental exposure of secrets.
#
# For local development only: Copy this file to .env and fill in your values

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# ✅ Replit: Automatically set via database provisioning
# 🏠 Local: Set your PostgreSQL connection string
DATABASE_URL=postgresql://username:password@localhost:5432/ovmpro

# =============================================================================
# PAYMENT PROCESSING (STRIPE) - OPTIONAL
# =============================================================================
# 📝 Only required if enabling Stripe payment processing
# Backend API key for processing payments
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Frontend publishable key for Stripe.js
VITE_STRIPE_PUBLIC_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# EMAIL SYSTEM (MICROSOFT 365 OAUTH2) - OPTIONAL
# =============================================================================
# 📝 Only required if enabling email notifications via Microsoft 365
# Azure App Registration credentials for Microsoft Graph API
AZURE_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
AZURE_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
AZURE_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# =============================================================================
# EXTERNAL APIs - OPTIONAL
# =============================================================================
# 📝 DVLA Vehicle Lookup API (UK vehicle data)
DVLA_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 📝 Google Maps API for mileage calculation and address autocomplete
GOOGLE_MAPS_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# Environment setting (development/production)
# ✅ Replit: Automatically set by deployment
NODE_ENV=development

# Server port
# ✅ Replit: Must be 5000 (automatically set)
PORT=5000

# Application URL for payment links
# 📝 Set to your custom domain in production (optional)
APP_URL=https://yourdomain.com

# JWT secret for authentication
# ⚠️  REQUIRED for production! Generate a secure random string (32+ chars)
# 🏠 Local/Dev: Will use auto-generated default (see console warning)
JWT_SECRET=your-secure-random-jwt-secret-key-here

# Object storage directory (for file uploads)
# ✅ Replit: Automatically configured via object storage
# 🏠 Local: Set path to private files directory
PRIVATE_OBJECT_DIR=/path/to/private/files

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# REPLIT DEPLOYMENT:
# 1. Use Replit Secrets tab to add sensitive keys (never commit secrets!)
# 2. DATABASE_URL is auto-provisioned with Replit PostgreSQL
# 3. PORT and NODE_ENV are automatically set on deployment
# 4. Generate a secure JWT_SECRET for production
# 
# LOCAL DEVELOPMENT:
# 1. Copy this file to .env
# 2. Set DATABASE_URL to your local PostgreSQL
# 3. Add any optional API keys you need
# 
# PRODUCTION (OTHER PLATFORMS):
# 1. Set NODE_ENV=production
# 2. Generate a secure random JWT_SECRET (at least 32 characters)
# 3. Use your actual domain in APP_URL
# 4. All API keys should be production/live keys, not test keys
# 5. Ensure PostgreSQL database is created and accessible
# =============================================================================